sonar.projectKey=thiscantbeserious_pi-rs
sonar.organization=thiscantbeserious

# Workspace layout (ADR 0011): sources live under crates/*/src, tests under
# crates/*/tests. The root tests/ dir holds e2e scripts only. Only crates
# with a tests/ dir are listed (pi-replay and pi-rs have inline tests only).
sonar.sources=crates/pi-core/src,crates/pi-protocol/src,crates/pi-replay/src,crates/pi-rs/src
sonar.tests=crates/pi-core/tests,crates/pi-protocol/tests
sonar.sourceEncoding=UTF-8
